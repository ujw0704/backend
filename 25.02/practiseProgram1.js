let fs = require("fs");
let data = fs.readlineSync("demo.txt");
let str = data.tostring();
let arr = split ("|");
let emp ={id:arr[0] ,name: arr[1], salary: arry[2]};
console.log( emp.id, emp.name,emp.id);